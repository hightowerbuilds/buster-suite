# Browser Workbench

This subsystem is intentionally not part of the core IDE shell.

The default product goal is a lightweight editor-first environment. An embedded browser is useful
for some workflows, but it should remain optional so users can keep the base app lean and rely on
the browser they already have installed.

The code in this directory is retained as optional infrastructure for a future add-on or extension
surface once panel-capable extensions exist.
