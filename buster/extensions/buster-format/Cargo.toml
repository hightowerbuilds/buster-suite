[package]
name = "buster-format"
version = "0.1.0"
edition = "2021"
description = "Buster reference extension — auto-format files via CLI formatters"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "s"
lto = true
strip = true
