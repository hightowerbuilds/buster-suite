[package]
name = "buster-sandbox"
version = "0.1.0"
edition = "2021"
description = "Allowlist-based command execution sandbox for the Buster IDE"

[dependencies]
thiserror = "2"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
tempfile = "3"
