[package]
name = "buster-remote"
version = "0.1.0"
edition = "2021"
description = "Remote development bridge for the Buster IDE — async SSH, workspace mirroring, connection management"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
