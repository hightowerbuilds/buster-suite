# Daily Summary — 2026-04-13

Two sessions today. Morning: blog theme system. Afternoon/evening: full 10-section release roadmap sweep plus terminal input fix.

---

## Session 1: Blog Mode Theme System

Settings-driven theme selection with a token resolver architecture that eliminates CSS specificity bugs during live switching. Newspaper theme (NYT-inspired) with Georgia serif, Old English masthead, justified text, drop caps. Token resolver (`blog-themes.ts`) replaces CSS class toggling with pure functions per variant.

---

## Session 2: Release Roadmap (10 Sections)

Worked through the entire release roadmap end-to-end. 12 commits, 54+ files changed, ~2400 lines added.

### 1. Error Handling Sweep (done)
- Created centralized `notify.ts` (showError/showInfo/showSuccess/logWarn) with in-memory ring buffer
- Replaced 49 silent catch blocks across 9 files
- Added `ConsolePanel.tsx` — canvas-rendered scrollable log viewer (Ctrl+` then L)
- LSP crash recovery with exponential backoff (3 retries, 2s/4s/8s) and "crashed" state with clickable restart
- Fixed Cmd+W closing window instead of tab (custom menu item replaces PredefinedMenuItem)

### 2. Loading States (done)
- CSS `.spinner` utility with `@keyframes spin`
- Status bar: animated dots for LSP Starting/Syncing/Loading via timer signal
- Git panel: spinner + "Syncing" label during push/pull/fetch
- Command palette: "Searching..." indicator during workspace search
- Extensions page: loading states during list fetch, toggle, install
- Settings: success toast on theme import

### 3. Distribution Pipeline (done — certificates pending)
- GitHub Actions release workflow (`.github/workflows/release.yml`) — builds macOS ARM+Intel, Windows NSIS, Linux deb+AppImage via tauri-action
- Tauri updater plugin wired in (Cargo, config, capability, frontend package)
- Platform bundler config (macOS 10.15 min, DeveloperTool category, deb dependencies)
- CI switched from npm to Bun

### 4. Find/Replace Completion (done)
- `engine.findAll(query, opts)` — search logic moved from UI to engine for Vim/programmatic reuse
- Current match highlighting — brighter + border via `currentSearchIdx` in store, piped through renderer
- Vim `/` and `?` open find panel; `n`/`N` cycle matches (were TODOs before)
- Vim `:s/find/replace/g` parsed in command palette line mode with regex + capture group support
- Alt+C / Alt+R shortcuts toggle case/regex while find panel is open
- Replace-all wrapped in `beginUndoGroup()`/`endUndoGroup()` for single Cmd+Z
- Invalid regex: red border + "Bad regex" text; active toggle buttons get accent highlight

### 5. Context Menus (done)
- Reusable `ContextMenu.tsx` component (position-fixed, click-outside/Escape dismiss, viewport clamp)
- Editor: cut/copy/paste/select all (cut/copy disabled when no selection)
- Tab bar: close/close others/close all/rename/copy path
- Terminal: copy/paste/clear
- Git panel: stage/unstage/view diff/open file/copy path
- `CanvasSurface` gained `onContextMenu` and `children` props

### 6. Security Fixes (done — wasmtime upgrade deferred)
- `@chenglou/pretext` 0.0.4 → 0.0.5 (DoS fix)
- Custom shell word parser replaced with `shlex` crate
- Debug adapter commands validated against `AdapterRegistry` allowlist before launch
- `cargo audit` run: 16 vulns in wasmtime v29; upgrade to v36+ is major, deferred

### 7. Extract BusterProvider Actions (done)
- Created `buster-actions.ts` (646 lines) with `createBusterActions(deps)` factory
- BusterProvider reduced 941 → 314 lines (store creation + effects + listeners only)
- `ActionDeps` interface for dependency injection — actions testable without full store
- Fixed debug event listener type mismatch (was passing setStore directly)

### 8. Sandbox Debugger Commands (mostly done)
- Adapter registry, validation, rejection, and security logging all completed in Section 6
- Custom adapter paths config deferred

### 9. Legal & Governance Files (done — CHANGELOG deferred)
- MIT `LICENSE` file
- `SECURITY.md` with disclosure policy and scope
- `CONTRIBUTING.md` with prerequisites, setup, project structure, code style, PR guidelines
- GitHub issue templates (bug report, feature request) + PR template

### 10. Accessibility Pass (partially done)
- ARIA `role` and `aria-label` added to canvas chrome (tab bar, status bar, dock bar)
- CanvasChrome gained `role` and `aria-label` props
- Verified: focus-visible indicators, skip links, keyboard navigation, terminal tabindex already solid
- Deferred: VoiceOver e2e test, high-contrast mode, help dialog

### Terminal Input Fix (in progress)
- Bug: backspace moves cursor forward visually while deleting in production WKWebView
- Root cause: two-path input (keydown for specials, textarea input event for printable chars) has WKWebView event ordering issues
- Fix: capture ALL printable characters directly in `handleKeyDown` via `e.key`, bypass textarea input event entirely
- IME/composition input handled separately with `isComposingInput` flag
- Built fresh DMG — needs user testing

---

## Production Build

Built `Buster_0.1.0.dmg` (15MB) and placed on Desktop. Unsigned — requires right-click > Open on macOS.

---

## Files Changed

**Created:** 14 new files
- `.github/workflows/release.yml`, `.github/ISSUE_TEMPLATE/bug_report.md`, `.github/ISSUE_TEMPLATE/feature_request.md`, `.github/pull_request_template.md`
- `LICENSE`, `SECURITY.md`, `CONTRIBUTING.md`
- `src/lib/notify.ts`, `src/lib/buster-actions.ts`
- `src/ui/ConsolePanel.tsx`, `src/ui/ContextMenu.tsx`
- `growth/daily-summary/2026-04-13.md`, `growth/past-deep-thoughts/release-roadmap.md`

**Modified:** 40+ files across frontend, backend, styles, config, and CI
